apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "static-service.fullname" . }}-test-connection"
  labels:
    {{- include "static-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "static-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
