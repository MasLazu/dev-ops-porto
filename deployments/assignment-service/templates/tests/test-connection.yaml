apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "assignment-service.fullname" . }}-test-connection"
  labels:
    {{- include "assignment-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "assignment-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
